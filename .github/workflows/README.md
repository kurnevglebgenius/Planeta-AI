# Workflows

`foundation.yml` runs clean-install web and API checks. Contract, database and secret-scanning jobs will be added when those implementations exist. No production credentials are required for current checks.
