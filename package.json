{
  "name": "planeta-ai",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/api-client"
  ],
  "scripts": {
    "lint": "npm run lint -w @planeta/web",
    "typecheck": "npm run typecheck -w @planeta/web",
    "build": "npm run build -w @planeta/web",
    "test:db": "node supabase/tests/workforce-access.mjs && node supabase/tests/auth-bootstrap.mjs"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8"
  }
}
